def main(): int {
  int count = 0;
  bool exit = false;

  while (!exit) {
    count = count + 1;
    print('count= ');
    println(count);
    if (count == 10) {
      exit = true;
    } else {
      exit = false;
    }
  }

  return 0;
}
